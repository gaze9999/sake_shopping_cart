let itemList = $('.itemList_list .test'),
    itemTree = $('.itemList_tree'),
    treeTotal = $('.tree_total'),
    dataLength,
    regionName = [],
    itemName = [],
    breName = [];

function getItem(id) {
    $.ajax()
}